spring:
  liquibase:
    enabled: true
    changelog: db/changelog/liquibase-changelog.xml
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
